[project]
name = "python-starter-project"
version = "0.1.0"
description = "Clean Python starter project for TS/JS developers."
readme = "README.md"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["src"]
